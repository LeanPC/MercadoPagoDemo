package com.example.leandropayments

class Constants {
    companion object {
        /**
         * The max input chars in amount fields
         */
        val MAX_AMOUNT_FIELD_CHARS = 12

        /**
         * The max decimal digits in amount fields
         */
        val AMOUNT_FIELD_DECIMAL_DIGITS = 2
    }
}